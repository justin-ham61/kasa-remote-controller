#ifndef INIT_H
#define INIT_H

#include "config.h"

void initializeFromList(const char* ips[]){
    int length = sizeof(ips)/sizeof(ips[0]);
    for(int i = 0; i < length; i++){
        
    }
}

#endif